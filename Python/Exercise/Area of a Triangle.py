b = float(input("Enter the length of the base of the triangle : "))
h = float(input("Enter its heights : "))

area = (b * h) / 2
print("The base lengths {0} and height {1}, so the area of the triangle is {2}".format(b,h,area))